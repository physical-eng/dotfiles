set shellslash
set runtimepath+=~/.dotfiles/vim

runtime! base/*.vim

if DeinIsInstalled()
    runtime! extend/*.vim
endif

if !filereadable ("~/.vimrc.local")
    call system("cp ~/.dotfiles/vim/vimrc.local ~/.vimrc.local")
endif

source ~/.vimrc.local
